%YAML:1.0

# Project
project_name: "lviorf"

#common parameters
imu_topic: "/bynav/imu/data"  #IMU话题
image_topic: "/m_gmsl_1/image_compressed"   # 图像话题
point_cloud_topic: "lviorf/lidar/deskew/cloud_deskewed"

# Lidar Params
use_lidar: 1                     # whether use depth info from lidar or not
lidar_skip: 1                    # skip this amount of scans
align_camera_lidar_estimation: 1 # align camera and lidar estimation for visualization


# lidar to camera extrinsic
lidar_to_cam_tx: 0.05
lidar_to_cam_ty: -0.07
lidar_to_cam_tz: -0.07
lidar_to_cam_rx: 0.0
lidar_to_cam_ry: 0.0
lidar_to_cam_rz: -0.04


# camera to ros standard frame(front-left-up) extrinsic, Theoretically, it is a fixed value
# for xiamen datasets(front-left-up)
cameraToROSStandard: !!opencv-matrix
   rows: 3
   cols: 3
   dt: d
   data: [0, -1, 0,
         0, 0, -1,
         1, 0, 0]

# cameraToROSStandard: !!opencv-matrix
#    rows: 3
#    cols: 3
#    dt: d
#    data: [0, 0, 1,
#          -1, 0, 0,
#          0, -1, 0]
# camera model PINHOLE，MEI
model_type: PINHOLE
camera_name: camera

# Mono camera config
image_width: 1920
image_height: 1080
distortion_parameters:
   k1: -6.4491587843
   k2: 64.9696548807
   p1: 0.0012128361
   p2: 0.0002983553
projection_parameters:
   fx: 1934.8593192748
   fy: 1935.5269145697
   cx: 946.4455094856
   cy: 481.4320261366

fisheye_mask: "/config/fisheye_mask_720x540.jpg"

#imu parameters       The more accurate parameters you provide, the worse performance
acc_n: 3.9939570888238808e-02         # accelerometer measurement noise standard deviation.
gyr_n: 1.5636343949698187e-02         # gyroscope measurement noise standard deviation.
acc_w: 6.4356659353532566e-03        # accelerometer bias random work noise standard deviation.
gyr_w: 3.5640318696367613e-03       # gyroscope bias random work noise standard deviation.
g_norm: 9.805       #

# Extrinsic parameter between IMU and Camera.
estimate_extrinsic: 1   # 0  Have an accurate extrinsic parameters. We will trust the following imu^R_cam, imu^T_cam, don't change it.
                        # 1  Have an initial guess about extrinsic parameters. We will optimize around your initial guess.
                        # 2  Don't know anything about extrinsic parameters. You don't need to give R,T. We will try to calibrate it. Do some rotation movement at beginning. 
#Rotation from camera frame to imu frame, imu^R_cam
extrinsicRotation: !!opencv-matrix
   rows: 3
   cols: 3
   dt: d
   data: [0.960316, -0.278479, 0.0155607, 
          0.278413, -0.960441, -0.00635433, 
          0.0167147, -0.00176987, 0.999859]

#Translation from camera frame to imu frame, imu^T_cam
extrinsicTranslation: !!opencv-matrix
   rows: 3
   cols: 1
   dt: d
   data: [-13.4155, 1.06769, -1.15495]

#feature traker paprameters
max_cnt: 250            # max feature number in feature tracking
min_dist: 20            # min distance between two features 
freq: 20                # frequence (Hz) of publish tracking result. At least 10Hz for good estimation. If set 0, the frequence will be same as raw image 
F_threshold: 1.0        # ransac threshold (pixel)
show_track: 1           # publish tracking image as topic
equalize: 1             # if image is too dark or light, trun on equalize to find enough features
fisheye: 0              # if using fisheye, trun on it. A circle mask will be loaded to remove edge noisy points

#optimization parameters
max_solver_time: 0.035   # max solver itration time (ms), to guarantee real time
max_num_iterations: 10   # max solver itrations, to guarantee real time
keyframe_parallax: 10.0  # keyframe selection threshold (pixel)

#unsynchronization parameters
estimate_td: 0           # online estimate time offset between camera and imu
td: 0.0                   # initial value of time offset. unit: s. readed image clock + td = real image clock (IMU clock)

#rolling shutter parameters
rolling_shutter: 0       # 0: global shutter camera, 1: rolling shutter camera
rolling_shutter_tr: 0    # unit: s. rolling shutter read out time per frame (from data sheet). 

#loop closure parameters
loop_closure: 1                    # start loop closure
skip_time: 0.0
skip_dist: 0.0
debug_image: 0                      # save raw image in loop detector for visualization prupose; you can close this function by setting 0 
match_image_scale: 0.5
vocabulary_file: "/config/brief_k10L6.bin"
brief_pattern_file: "/config/brief_pattern.yml"
